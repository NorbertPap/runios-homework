async function getPhotos(count, page, tag) {
    // reimplement with SQL db
}

async function deletePhotoById(id) {
    // reimplement with SQL db
}

module.exports = {
    getPhotos: getPhotos,
    deletePhotoById: deletePhotoById
};
